class Instructions {
  
  void draw() {
    background(#FFFFFF);
    
    fill(#EC7063);
    textSize(40);
    text("INSTRUCTIONS", 150, 100);
    
    fill(0);
    textSize(20);
    text("-Use the LEFT and RIGHT keys to move", 100, 140);
  }
}
