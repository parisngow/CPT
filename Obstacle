public class Obstacle extends Cell {
  
  public Obstacle (PVector loc, PVector speed) {
    super(loc, speed);
  }
  
  void draw() {
    noStroke();
    fill(0);
    rect(getXLoc(), getYLoc(), 20, 50);
  }
}
